# Arat -- Birden fazla olabilir. " def __DragonSoulInfoString (self, dwVnum): " üstünde olanı bulun:
		self.ShowToolTip()

# Üzerine ekle:
		if chr.IsGameMaster(player.GetMainCharacterIndex()):
			self.AppendTextLine(localeInfo.ITEM_VNUM_TOOLTIP % (int(itemVnum)), self.SINIRSIZ_COLOR)
# Örnek: https://prnt.sc/pg2y2a

# Arat:
	NEED_SKILL_POINT_COLOR = 0xff9A9CDB

# Altına ekle:
	SINIRSIZ_COLOR = 0xffFFFF00